{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/webhook", "dest": "/api/webhook" },
    { "src": "/setWebhook", "dest": "/api/webhook" },
    { "src": "/health", "dest": "/api/webhook" }
  ]
}
